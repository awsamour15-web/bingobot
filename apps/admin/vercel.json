{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @fidel/shared build && pnpm --filter @fidel/admin build",
  "outputDirectory": "apps/admin/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
